# Frontend
# Leave empty when frontend and backend are served from same host.
VITE_API_BASE_URL=

# Backend
PORT=8787
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=8h
# Used only when DATA_FILE does not exist yet (initial seed).
ADMIN_PASSWORD=mrpauto2025
CORS_ORIGIN=http://localhost:5173
DATA_FILE=server/data/db.json
